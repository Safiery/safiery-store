{
  "name": "safiery-store",
  "version": "1.0.0",
  "private": true,
  "description": "Safiery B2C + B2B ecommerce storefront — static site with Stripe checkout via Netlify Functions",
  "scripts": {
    "dev": "npx netlify-cli@latest dev",
    "deploy": "npx netlify-cli@latest deploy",
    "deploy:prod": "npx netlify-cli@latest deploy --prod"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "stripe": "^16.12.0"
  }
}
